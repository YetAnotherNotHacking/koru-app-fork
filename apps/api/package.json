{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload --app-dir .",
    "lint": "uv run ruff check . && uv run mypy .",
    "format": "uv run ruff format .",
    "start": "uv run uvicorn api.main:app --host 0.0.0.0 --port 8000 --app-dir ."
  },
  "dependencies": {}
}
